import React from 'react'; 



const initialState = {
  orders: [],
  isLoading: false,
};

export const orderReducer = (state = initialState, action) => {
  switch(action.type){
    default:
      return state
  }
}